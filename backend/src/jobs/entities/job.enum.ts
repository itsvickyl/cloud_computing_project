export enum JobStatus {
  OPEN = 'open',
  LAST = 'last stage',
  CLOSED = 'closed',
}

export enum JobType {
  FULL = 'full-time',
  PART = 'part-time',
  INTER = 'internship',
  CONTRACT = 'contract',
  TEMP = 'temporary',
}
