# TalentScope server in NESTJS

### Requirements

> Node 20^

> NestJS CLI (Optional but recommended)

### Local environment

1. Set up the `.env` file

2. `pnpm i` to install all the dependencies

3. `pnpm start:dev` to start the dev server

### Demo environment

Demo environment
